- "Manual project setup over cargo-generate template (template uses outdated zellij-tile 0.41.1)"
- "Request all three permissions upfront in load() since dialog is cached by plugin URL"
- "Edition 2021 over 2024 (battle-tested for wasm32-wasip1, no value from newer edition)"
- "Super p (Cmd+P) for toggle keybind — replaces Alt+s focus-only approach. Full show/hide cycle via hide_self()/show_self(false)"
- "Yellow for exited sessions instead of red — exited is a recoverable state (resurrectable), not an error condition"
- "Verbosity defaults to Full — minimal is opt-in via KDL config. Full shows tab count + active command, minimal shows only dot + name"
- "Color palette indices (0=green, 3=yellow, 4=blue, 7=gray) map through Zellij theme — not hardcoded RGB. Colors adapt to whatever theme the user has configured"
- "Active command shows basename only — full path would be noisy and overflow the narrow sidebar"
- "Legacy pipe message 'focus_sidebar' preserved alongside new 'toggle_sidebar' — avoids breaking existing keybind configs during migration"
